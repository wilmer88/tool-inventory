   <article class="grid-container">
      <div class="grid-x align-center">
        <div class="cell medium-9">
            <br>
          <div class="blog-post">
            <h4>Created Date: <small>{{createdAt}}</small></h4>
             <br>
                  <a class="button secondary" href="/departmentArrayItems/{{routeName}}">Back</a>
                 
             <br>
                   <div class="callout">
                   
               <p><strong>Department/ Crew:</strong></p><h3> <bold>{{name}}</bold></h3>
              </div>
              
             <div class="callout">
               <p><strong>Supervisor/ Manager:</strong> </p> <h3><bold>{{supervisor}}</bold> </h3>
              </div>
             <div class="callout">
              <p><strong>Lead/ Spotter:</strong> </p>   <h3>{{lead}} </h3>
              </div>

            <div class="callout">
              <ul class="menu simple">
                <li>Edited:{{updatedAt}}</li>
                {{!-- <li><input type="hidden" value="{{routeName}}" id="item-id" ></li> --}}
             
             </ul>   
            </div>
             <div class="callout">
             <a class="button expanded" href="/departmentEditPage/{{routeName}}">Edit</a>
              </div>
               <div class="callout">
           
             <a class="button secondary expanded" href="/departmentArrayItems/{{routeName}}">Back</a>
              </div>
            
               <a class="button warning secondary expanded" id="delete"  data-id="{{routeName}}">Delete Department</a>   
           
              
          </div>
        </div>
      </div>
   </article>

<script>
  $("#delete").on("click", function (){
    const confirmDelete = confirm("All items in this department will be deleted do with to continue?")
    if(confirmDelete){
      console.log($(this).data("routeName"))
          const routeToDelete = $(this).data("id");

    $.ajax({
      url: `/api/deleteDepartment/${routeToDelete}`,
      method: "DELETE"
    }).then(function (){
     window.location.replace("/");
     alert("successfully deleted selected item")
    })

    }

  })
</script>