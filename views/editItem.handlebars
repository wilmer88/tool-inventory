  <form id="monkey-code">
          <input type="text" value="{{name}}" id="name-input" aria-label="name of item">     
          <input type="text" value="{{placement}}" id="placement-input" aria-label="department placement">
          <input type="text" value="{{serial}}" id="serial-num" aria-label="serial number" >
          <input type="text" value="{{count}}" id="exact-count" aria-label="count" >
          <input type="hidden" value="{{id}}" id="item-id" >
   <button type="submit" class="button">update</button>
        </form>
    
<script>
  $("#monkey-code").on("submit", function (event) {
    event.preventDefault();
  const name = $("#name-input").val();
   const placement = $("#placement-input").val();
  const serial = $("#serial-num").val();
  const Count = $("#exact-count").val();
  const id = $("#item-id").val();
console.log(id);
$.ajax({
 url:`/api/item/${id}`,
 method: "PUT",
 data: {
   name: name,
   placement: placement,
   serial: serial,
   count: Count,
 },
}).then(function () {
window.location.replace(`/item/${id}`);
}).catch(err => {
 console.log(err)
  res.status(500).json({
      error: true,
      data: null,
      message: "faild to update item",
    });
})
})
</script>